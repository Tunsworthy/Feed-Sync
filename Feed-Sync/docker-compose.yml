version: '3.8'

services:
  backendapi:
    container_name: feedsync
    image:  ghcr.io/tunsworthy/feedsync:latest
    env_file:
      - .env
    depends_on:
      - postgres
    networks:
      baby_org_net: null
networks:
  
  baby_org_net:
    name: baby_org_net
    internal: true

